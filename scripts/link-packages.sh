#!/usr/bin/env bash
# This script is kept for reference but is no longer needed.
# Local packages (game-engine, chess-shared) are now resolved automatically
# via npm workspaces. Running `npm install` at the repository root is sufficient.
echo "Local packages are managed via npm workspaces. No manual linking required.";
